# restructure-modules 测试清单

分支 `restructure-modules` 相对 `main` 要测的东西。仓库没有 `master`，默认分支是 `main`。

- **起点**：`main` 的 `ed96578`（2026-09-23「对话改版：问卡与痕」）。这之前的功能在 main 上已经有真机记录，见 CLAUDE.md「现状」。
- **终点**：`960efe9`（2026-09-24）之后又加了抓取页（§6）。
- **这个分支还没上过真机。** 前 3 个提交在本地编译过、跑过单测、打过包。后 5 个是在云端会话里写的，那里的网络策略拦了 `dl.google.com`，app 没编译过。后 5 个里改了代码的有 3 个：记账提示词、Markdown、画图。

## 改了什么

| 提交 | 内容 | 测什么 |
|---|---|---|
| `5f01cd3` 挪包 | 分成 agent / reminder / ledger / shared 四块，全类名都变了；提醒库改名 `reminder.db`，版本回到 1 | 回归（§2） |
| `71550a2` 接头 | agent 只经 `Feature` / `FeatureUi` 接模块；`MainViewModel` 拆开；小组件、通知、速记改走路由 `Launch` | 回归（§2） |
| `7758556` 收尾 | 日期、星期、重复规则的人话写法各合成一份；巡检改名 `reminder_sweep` | 回归（§2.3） |
| `e37d513` `8bf4828` | 只改文档和注释 | 不用测 |
| `4e2246a` 记账提示词 | 转账给别人算支出，不作废；整理提示词升 organize-v4 | §3 |
| `88e4bd6` Markdown | 模型的回答按 Markdown 画 | §4 |
| `960efe9` 画图 | 模型写的 ```` ```svg ```` 画成图，点开能放大，深色下换色；提示词加「画图」一节；更早几轮的图折掉再喂给模型 | §5 |
| 抓取页 | 看监听连没连着、手动抓一下、看抓到的原文；「通知使用权」改成按组件查 | §6 |

## 1. 装机前（在电脑上）

- [ ] `./gradlew :app:testDebugUnitTest` 全过。离线单测一共 74 条，`LiveGatewayTest` 没设 `DAODIAN_LIVE=1` 时跳过。其中 37 条是这个分支加的：`MarkdownTest` 26 条、`FoldDrawingsTest` 3 条、`SvgColorsTest` 4 条、`LedgerFormatTest` 4 条。这 37 条以前只在单独的 JVM 工程里跑过，这次是第一次在 app 里跑。
- [ ] `./gradlew :app:assembleDebug` 通过。Markdown、画图、抓取页的代码这是第一次按 Android 编译（抓取页用到的 DAO 查询也是第一次过 Room 的 SQL 检查），新依赖 `com.caverock:androidsvg-aar:1.4` 也是第一次拉。要报错，最可能在 `MarkdownText.kt`（链接用的 `LinkAnnotation`）和 `SvgBlock.kt`。
- [ ] （可选）卸载前把要留的东西拷出来，比如调研样本：
  `adb exec-out run-as com.abc.daodian.debug cat files/pay_samples.imported.jsonl > pay_samples.jsonl`
- [ ] **连数据一起卸载**，再装新包：`adb uninstall com.abc.daodian.debug` → `./gradlew :app:installDebug`。全类名和提醒库都改了，不能直接覆盖安装。卸载会清空这些：提醒、账本、对话、模型配置、收尾时刻、所有权限、桌面小组件。完整的表在 PROJECT_STRUCTURE.md「卸载重装」。

## 2. 包结构重排的回归（`5f01cd3` `71550a2` `7758556`）

这三步的本意是行为一个都不变。下面挑的是两类：全类名一变就可能坏的，和接线改过的。括号里写为什么要测。

### 2.1 装好、配好

- [ ] 设置页从上到下：体检结论 → 模型服务 → 系统权限 → 提醒 → 记录 → 记账。以前「提醒」在最上面，现在这个顺序是故意改的。
- [ ] 体检逐条开到全绿：通知、精确闹钟、全屏通知、通知使用权、电池 / 应用启动管理。从系统设置回来，对应那条自己变成对勾（体检项现在由各模块的 `Feature.health` 给）。
- [ ] 配置页填火山方舟 → 点「测一下」能通过（`ProviderTest` 现在带着真实的提示词和全部工具，请求比以前大）。
- [ ] 抽屉：顶上「到点 · 回到对话」，中间提醒、记账各一张纸，底下是「设置」和「还差 N 项」。
- [ ] 库文件对：`adb shell run-as com.abc.daodian.debug ls databases` 里有 `reminder.db`、`chat.db`、`ledger.db`，没有 `daodian.db`。
- [ ] 巡检改名了。WorkManager 的库里有 `reminder_sweep`，没有 `daodian_sweep`：
  ```bash
  for f in androidx.work.workdb androidx.work.workdb-wal androidx.work.workdb-shm; do
    adb exec-out run-as com.abc.daodian.debug cat databases/$f > $f; done
  sqlite3 androidx.work.workdb "select name from WorkName"
  ```

### 2.2 对话和提醒

- [ ] 对话空状态的四条例句还在，点一条能发出去（例句现在来自 `Feature.examples`）。
- [ ] 说「明天早上8点提醒我带伞」→ 出一道痕「✓ 提醒 … · 带伞 ›」。然后跑 `adb shell dumpsys alarm | grep -B2 -A6 daodian`，里面的 receiver 是 `com.abc.daodian.reminder.scheduling.AlarmReceiver`。
- [ ] 点这道痕进编辑页，「原话」下面的「依据」有值（依据现在由提醒自己从 `chat.db` 里取出来解析）。
- [ ] 说「明天下午给妈妈打电话」→ 出问卡 → 点一个选项 → 出痕，闹钟也排上。
- [ ] 说「hello」只回一句话，不建提醒。基础提示词和「提醒的规则」拆成两段、又拼了回去，应该和原来逐字节一样。
- [ ] 开飞行模式发一句 → 出错的地方有「手动填一条」→ 点了进新建提醒页（去处现在由 `Feature.manualEntry` 给）。
- [ ] 真响一次：定一条两分钟后的 → 准点响，锁屏上弹出全屏页，全屏页上的日期是汉字写法 → 在通知上点「完成」→ 闹钟和通知都收掉。
- [ ] 点到点通知的正文，能打开 app（通知的去处改走 `Launch` 了）。
- [ ] 重启手机后跑 `dumpsys alarm`，闹钟还在（开机重排的 `RescheduleReceiver` 挪了包，manifest 里的名字是跟着改的）。

### 2.3 提醒的文字（`7758556` 把重复的几份写法合成了一份）

- [ ] 重复规则的人话。在对话里建这三条：「每周一、三早上8点跑步」「每月最后一天交房租」「每天晚上10点吃药」。痕、列表、编辑页上分别写成「每周一、三」「每月最后一天」「每天」。
- [ ] 痕、列表、小组件上写的星期几和日子对得上。
- [ ] 当天事项：说「今天把报销交了」→ 列表里是当天事项的写法。再把收尾时刻拨到两分钟后 → 准点响 → 点通知进列表页。
- [ ] 顶栏印章的纸签和设置页「模型服务」那一行，网关只写主机名。
- [ ] 投递日志页能打开：设置 → 记录 → 投递日志。
- [ ] 在列表里点圈完成、左滑删除、点撤销，每一步之后小组件都跟着变（刷新小组件现在是 `Reminders` 在每次写完之后自己喊的）。

### 2.4 小组件和桌面速记

- [ ] 长按桌面把小组件加回来，抬头是「到点 +」。
- [ ] 点抬头进提醒列表，点某一条进这条的编辑页，点空白处进对话页。这三个去处现在都走路由。
- [ ] 点墨印 → 纸从小组件那一块长出来（`WidgetFrame` 现在按包名找小组件）→ 说或打一句 → 记好之后纸自己收起，小组件亮「刚记下」。
- [ ] 在速记里说一句需要问的，比如「明天下午给妈妈打电话」→ 交给对话页：app 打开，这句话已经发出去，问卡出现在对话里（`Launch` 带着 `say`）。
- [ ] 在对话页里建一条 → 回到桌面，小组件上已经有这条。
- [ ] 深链：app 开着、关着各试一次。开着的时候走的是 `onNewIntent`。
  `adb shell am start -n com.abc.daodian.debug/com.abc.daodian.MainActivity --es com.abc.daodian.launch.ROUTE reminder/list`

### 2.5 记账

- [ ] 开了通知使用权之后，付一笔真钱（微信、支付宝、银行卡都行）→ 记账页里出现这一笔（听通知的 `PaySampler` 挪了包，使用权是按新的组件名授的）。
- [ ] 设置 → 记账 → 点「现在整理一次」，能跑完。这一笔要么归好了类，要么进了「未归类」。
- [ ] 记账三层：首页 → 点一个类别 → 点一笔，每一层的日子和时刻都对（几份日期函数合成了 `LedgerDays`）。
- [ ] 在对话里问「这个月花了多少」，回答是人话。
- [ ] 在对话里改账：
  - 改一笔，比如「#1 那笔是午饭」→ 痕上写这一笔改成了什么，点它直接进账单详情；
  - 一次改两笔以上 → 痕上写「改了 N 笔 ⌄」，展开后逐笔列出来。
- [ ] 对账。有没认出来的笔时，记账页上点「现在就说 ›」→ 对话页开一轮对账，上面的分隔线写「每晚对账」（分隔线的特判删掉了，现在靠通用规则取名）。每晚 21:30 的通知上点「现在」，结果也一样。
- [ ] 杀掉 app 重开，历史里记账的那几轮画成「✓ 记账 改了 N 笔 ⌄」。

## 3. 记账提示词（`4e2246a`）

起因是一次对账：用户答「转账给某人」，模型当成自己账户之间的转移，直接作废了。

- [ ] 在对话里说「#N 那笔是转给我妈的」→ 这笔**没被作废**，归成了支出（拿不准归哪类会归「其他」）。账本里还在，算进这个月的支出。
- [ ] 对账的问卡里答「转给朋友的」→ 结果同上。
- [ ] 该作废的还能作废：「#N 那笔重复了」→ 作废。
- [ ] 自己账户之间挪钱（还信用卡、转进余额宝）还是算转移，不算花钱。
- [ ] 后台整理：新来一条转账给别人的通知 → 整理后是支出，这一批的 `parsedBy` 以 `@organize-v4` 结尾：
  ```bash
  for f in ledger.db ledger.db-wal ledger.db-shm; do
    adb exec-out run-as com.abc.daodian.debug cat databases/$f > $f; done
  sqlite3 ledger.db "select id, direction, amount, summary, parsedBy from txn order by id desc limit 5"
  ```

## 4. 正文 Markdown（`88e4bd6`）

模型写什么界面就画什么，没有提示词约束它，所以要挑容易让它写出格式的问题来问，比如：

- 「分几点说说这个月怎么省钱」：会出标题、列表、粗体
- 「用表格列一下这个月每类花了多少」：会出表格
- 「写一段 Kotlin 的 hello world」：会出代码块
- 「推荐几个记账的网站，带上链接」：会出链接

要看的：

- [ ] 粗体、斜体、删除线、行内代码的样子，特别是中文粗体用系统字体好不好看。
- [ ] 标题；有序和无序列表（嵌套最多三层，圆点依次是 • ◦ ·）；引用（左边一道竖线）；分隔线。
- [ ] 表格：列宽合适，全是数字的列靠右；列多了能横着滑，右边有渐隐。
- [ ] 代码块不自动换行，能横着滑。
- [ ] 链接是朱砂色，点了用浏览器打开。不是 http / https 的链接点不动。
- [ ] **「#12 那笔」这种不会变成标题**。记账里常这么写，`#` 后面没有空格就当普通字。
- [ ] 流的时候：
  - 逐字淡入和光标都还在；
  - `**` 不会先露出来再消失；
  - 表格先出表头，再一行一行长；
  - 只有「- 」或「1. 」的那一行先不画。
- [ ] 刚流完和重启后读回的历史，画出来一样。
- [ ] 深色模式下都看得清，特别看代码块底色、表格线、引用线。
- [ ] 没有格式的普通回答和以前一样，单个换行还在。
- [ ] 痕、问卡、思考块不受影响。

## 5. 画图（`960efe9`）

- [ ] 问「这个月钱花在哪，画个图看看」→ 先有一句话结论，再有一张铺满宽度的图。模型不画，就说明提示词里「画图」那一节没起作用，记下来。
- [ ] 图上的数和它刚查到的一致。数字是模型照查账结果抄上去的，可能抄错。这是选静态图时接受的代价，抄错了记下来。
- [ ] 中文字正常显示，不是方块（AndroidSVG 用的是系统字体）。
- [ ] 画的过程中：先出一个虚线框，里面是墨条和「在画图」；写完换成图，高度不跳。
- [ ] 点图进全屏：两指能缩放，放大后能拖，双击放大或还原，点一下或按返回键收起。
- [ ] 深色模式：图是透明底，字和线是浅墨色，数据的颜色换成深色色板里对应的。切换深浅色之后，已经画出来的图也跟着换。
- [ ] 没要图的时候不画：问「这个月花了多少」只回数字。
- [ ] 画到一半点「停」→ 这一整轮撤掉，原话退回输入框，不留一个一直在转的虚线框。
- [ ] 画不出来时退回原文：让它「用 svg 代码块写一个标签没闭合的坏图」→ 显示「这张图画不出来，原文在这里：」，下面是代码块，app 不崩。
- [ ] 接着说「把刚才那张图换成柱状图」→ 它会重新查一遍再画。更早几轮里的图喂给模型前折成了一句话，它看不到原图，所以要重查。
- [ ] 杀掉 app 重开，历史里的图还在。
- [ ] 对话里有好几张图时，上下滑动不卡。

## 6. 抓取页

入口两个：设置 → 记账 →「抓到的通知」；记账总览最底下「少了一笔？看看抓到的通知 ›」。

- [ ] 页面样子：上面一张纸（通知使用权、监听、最近一次实时收到、待整理），中间一个墨色大按钮「现在抓一下」，下面按天分组的原文。
- [ ] 监听那一行写「连着 · 今天 HH:MM 起」。设置页「抓到的通知」那行是灰字。
- [ ] 付一笔真钱 →「最近一次实时收到」当场变成刚才的时刻，列表最上面多一条，标「实时」「待整理」。
- [ ] 通知还挂在通知栏里时点「现在抓一下」→ 按钮下面写「通知栏里挂着 N 条，早就都存下了」。
- [ ] 把一条支付通知划掉，再点「现在抓一下」→ 抓不回来（这是系统的限制，页上写着）。
- [ ] 点「待整理 N 条」那行的「现在整理」→ 整理完，那几条的标签变成「进了 #N ›」，点它进那一笔的详情。
- [ ] 点一条原文 → 展开，看到全文、「通知发出 … · 抓到 …（晚 N 秒）」。
- [ ] 连着的时候点「重连」→ 先变红「断了」，一两秒后回到「连着」、时刻更新。**荣耀上灵不灵是这次最想知道的。**
- [ ] 监听断了的样子：在系统设置里把「到点」的通知使用权关掉 → 回到页面，第一行变红「没开」，按钮变成「先去开通知使用权」→ 点它去开 → 回来变对勾、监听连上。
- [ ] 杀掉进程（`adb shell am force-stop com.abc.daodian.debug`）后重开 app，直接进抓取页：监听是连着，还是红字「这次打开 app 以来还没连上过」？没连上的话点「现在抓一下」，看它能不能自己连上再扫。
- [ ] 手动抓到的在库里是 `tap`，整理前扫到的是 `organize`：
  `sqlite3 ledger.db "select id, capturedHow, state from raw_notification order by id desc limit 10"`（导出库的命令见 §3 最后一条）

## 7. main 上就没验过、这次顺手可以验的

这些不是这个分支改的，但卸载重装之后反正要从头走一遍，碰到了就一起看。来历见 CLAUDE.md「现状」。

- [ ] 深色模式：时间轴列表、设置页、编辑页、问卡。
- [ ] 问卡出来后不点选项，直接打一句话回答。
- [ ] 问卡等着的时候点「停」，问卡画成「没答」；杀掉进程再打开，读回来还是「没答」。
- [ ] 小组件拖成 4×2、4×3、4×4 时的版式。
- [ ] 断网或者 key 填错时，流式失败后退回非流式。
- [ ] 配置页开「先想一想再答」之后，网关收不收，有没有思考块出来。
- [ ] 48 小时放置测试。这是 M1 唯一还没拿到的硬证据。

## 出问题时带回来什么

- 截图。和动效有关的录屏。
- 闹钟：`adb shell dumpsys alarm | grep -B2 -A6 daodian`
- 模型的原文。Markdown 或图画错了的时候最有用，因为界面上看不到原文：
  ```bash
  for f in chat.db chat.db-wal chat.db-shm; do
    adb exec-out run-as com.abc.daodian.debug cat databases/$f > $f; done
  sqlite3 chat.db "select turnId, text from chat_items where kind = 'ASSISTANT' order by id desc limit 3"
  ```
- 这台 ROM 屏蔽了第三方 app 的 logcat，别指望看日志。
