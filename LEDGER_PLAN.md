# 记账 · 设计与执行计划

> 目标：付款通知自动变成流水，**由模型读懂、整理、归类**；有把握的一声不吭记好，没把握的每晚在对话里问你一次。
> 架构总纲仍以 [DESIGN.md](DESIGN.md) 为准（agent 框架见 §6.8），这份管记账这一块的「怎么设计、按什么顺序做」。
> 2026-09-19 起草（正则方案），**2026-09-23 推倒重写为模型方案，流程定稿；同日数据格式定稿（§10）；同日开始实现，代码地图和与计划的出入见 §11**。

---

## 1. 已定的决策

| 事项 | 决定 |
|---|---|
| 采集来源 | 支付宝、招商银行、掌上生活、建设银行（`com.chinamworld.main`，9-21 加） |
| **谁来读懂通知** | **模型**。代码只收集、存档、排期、校验、画界面；理解全交给 agent |
| 整理节奏 | 代码**每 3 小时**检查有没有待整理的原始通知，有才叫模型；**间隔可在设置里改** |
| 对账时刻 | 单独一个钟点，默认 21:30，可改；和「当天事项收尾」分开 |
| 隐私 | 流水（金额、商户、卡尾号）**原样发给模型网关**，不脱敏（用户选 a） |
| 自动归类 | 模型**有把握的直接归，不问**；没把握的攒到对账时问 |
| 商户记忆 | **只有用户确认过的才写**（点「好」或亲口说的）；模型自己归的只算那一笔，不固化 |
| 要问的归类 | 走输入框上方的授权条（和建提醒一样） |
| 「晚点」 | 固定 1 小时后再问；这期间对完了就当场取消 |
| 打扰上限 | 一天最多主动问一次（「晚点」那次不算）；没有待确认的就不问 |
| 类别 | **两层**。一级预设：支出 8 个（餐饮 / 交通 / 购物 / 日用 / 娱乐 / 住房 / 医疗 / 其他）+ 收入 4 个，新增要问；二级模型自己建、不问，每个一级下封顶 12（§10.2） |
| 看账 | 新增**只读**流水列表页（先出设计稿）；**改账一律走对话 + 授权条**，列表页不带编辑。界面上只看月支出、月收入、按一级类别分，细的都在对话里问 |
| 退款 | 算**退款那个月的一笔负数**，类别跟原笔走 |
| 存储 | `data/ledger/` 单独一个 `ledger.db`，不碰 `daodian.db` 和 `chat.db` |
| 调度 | 放在记账自己的包里，**不碰 `schedule/`** |

---

## 2. 采样得来的事实（9-18 ~ 9-23，真机）

### 2.1 四家各自给得出什么

| 来源 | 标题 | 给得出 | 给不出 | 样本原文 |
|---|---|---|---|---|
| 建行 | 动账提醒 | 卡尾号、到分钟的时间、支出/收入、金额 | **商户** | 您尾号4918的储蓄账户9月22日19时18分支出人民币197.00元。点击查看>> |
| 招行 | 招商银行 | 卡尾号、金额、消费/退款 | 商户、时间 | 信用卡通知：您尾号8837的招行信用卡消费319.40人民币。 |
| 掌上生活 | 交易提醒 / 退款提醒 | **商户**、渠道（支付宝-/财付通-）、金额 | 卡尾号 | 您在支付宝-山姆会员商店有一笔319.40人民币的消费已成功，点击查看详情【查资格达标100%中奖】 |
| 支付宝 | 交易提醒 | 金额、是否花呗 | 商户 | 你有一笔152.00元的支出，点击领取2元流量红包。使用花呗支付，请及时还款。 |

- **商户名只有掌上生活给**。招行信用卡的每一笔都会**同时**来招行 + 掌生两条（间隔 0.2~0.6 秒、金额一字不差）→ 必须合并成一笔。
- 建行储蓄卡消费**永远拿不到商户**，只能靠模型结合金额/时刻/历史猜，或者问。
- 真实出现过：**退款**（招行 1.49 + 掌生「退货已成功」）、**收入**（建行转入 1.00、活期结息 0.73）。
- 营销噪音不少：建行立减金 ×3、招行优惠券到期、「咖啡爱好者看这里」；还有标题正文全空的分组汇总通知（`ranker_group`）。

### 2.2 正文会被系统遮蔽（已查清机制）

- 现象：招行通知有时收到的是「敏感数据已隐藏」，过几秒再读同一条（key、postTime 分毫不差）就是全的。
- 源头：Android 15 对第三方通知监听的敏感内容遮蔽（`redact_sensitive_notifications_from_untrusted_listeners=true`）。替换文案是 framework 的 `android:string/redacted_notification_message`。豁免权限 `RECEIVE_SENSITIVE_NOTIFICATIONS` 是 `signature|role`，**我们永远拿不到**。跟锁不锁屏无关（锁屏那个 flag 是 false）。
- 谁判的：荣耀 SystemUI 里的 `HnNotificationAssistant`，两阶段：
  1. **同步、纯正则** —— 荣耀把 OTP 正则的前缀扩到了汉字，「尾号**8837**的」四位数前后都是汉字 → 被当成验证码 → 立刻打上 `key_sensitive_content=true`，递给我们的就是遮蔽版；
  2. **异步、上 TextClassifier** —— 中文语境词只认「密码 / 校验码 / 验证码」，消费通知一个都没有 → 撤掉标记 → 再读就是真的。线程池队列满了第二步会被跳过，那条就永远是遮蔽版。
- 实测：9-21 20:53 那条遮蔽后 **5 秒重扫就拿到了真正文**。之后几十条没再遮。
- 对策（已在采样器里）：遮蔽的 5s / 30s / 2min 各重扫一次、解锁时扫、手动补抓。

### 2.3 实时回调会丢

- 9-22 09:06 那笔 199.90（招行 + 掌生两条）**实时回调一条都没收到**，监听一直连着，是用户点「补抓」才捞回来的。22 条里丢 2 条，约 10%。推测是荣耀冻结了 app 进程。
- 结论：**不能只靠实时回调，必须定期扫通知栏兜底**。去重按 `key | postTime | 正文` 指纹，进程重启后从存档续上。
- 另：`adb install -r` 在这台机器上**不一定重启 app 进程**，装完要确认进程换了（`ps` 看启动时间），否则跑的还是旧代码。

---

## 3. 分工线

| | 干什么 | 为什么归它 |
|---|---|---|
| **代码** | 收通知、原样存档、到点唤醒、校验模型产出后落库、画界面 | 这些要 100% 可靠，不能有创造性 |
| **模型** | 是不是一笔账、多少钱、什么方向、哪个商户、几条通知是不是同一笔、退款挂哪笔、归哪类、哪些要问你 | 都是「看得懂就行」的活；正则追不上银行改文案 |

原始通知是**唯一真相、永不删除**。模型的产出是派生数据，可以整批重跑。

---

## 4. 整体流程

```
┌─① 采集（一直在跑）─────────────────────────────────┐
│ 通知到达 → 采样器 → raw_notifications（待整理）       │
│ 实时回调丢了的，靠解锁时扫通知栏、手动补抓捞回来       │
└────────────────────────────────────────────────┘
                       ↓
┌─② 整理（默认每 3 小时，可配置）──────────────────────┐
│ 代码先数：待整理的有几条？（不算最近 10 分钟到的）       │
│   0 条 → 睡，不花一分钱                              │
│   有   → 整理 agent 在后台跑一轮                      │
│          喂：这批原始通知 + 类别表 + 商户记忆             │
│             + 最近 30 天流水摘要                       │
│          它调 record_expenses → 代码过护栏（§7）→ 落库   │
│            ├ 有把握 → 自动归类                         │
│            ├ 没把握 → 待确认，附上它想问你的话            │
│            └ 不是账（营销、还款、空汇总）→ 原始通知标忽略 │
└────────────────────────────────────────────────┘
                       ↓
┌─③ 对账（每晚 21:30，可配置）───────────────────────┐
│ 先强制整理一次，把手上的清空                          │
│ 没有待确认的 → 不打扰                                │
│ 有 → 后台先写开场白 → 弹通知 [现在][晚点][今天算了]     │
│      开场白带一句「另外自动记了 N 笔，共 ¥X」            │
│      → 对话 → 授权条 → 你点「好」→ 落库 + 记商户记忆     │
└────────────────────────────────────────────────┘

┌─④ 随口（任何时候，在对话页直接说）───────────────────┐
│ 「刚才停车 5 块」→ 记   「山姆那笔是吃饭」→ 改          │
│ 「这个月吃饭花了多少」→ 查                             │
└────────────────────────────────────────────────┘
```

### 状态机（只能往一个方向走）

```
原始通知：待整理 ──→ 已整理 / 忽略 / 看不清
流水：    自动归类 ──→ 已确认
          待确认 ───┘
          任何状态 ──→ 作废（拆错、并错、不是账；不删，不进统计）
```

### 拿真实数据走一遍（9-22 晚）

| 时刻 | 发生什么 |
|---|---|
| 19:18~19:20 | 建行 197、招行 319.40、掌生「支付宝-山姆会员商店 319.40」先后到达，进原始表 |
| 21:00 整理 | 招行 + 掌生 → 合成 1 笔「山姆 ¥319.40」，「会员商店」够明显 → 自动归日用；建行 197 没商户 → 待确认，想问「19:18 建行 ¥197 是什么？」 |
| 21:30 对账 | 通知「今天 1 笔没认出来，另外自动记了 1 笔」 |
| 你回一句 | 「197 是理发」→ 授权条 → 好 → 归日用（没商户，不写商户记忆） |

---

## 5. 三个 agent 场景

| 场景 | 触发 | 授权 | 说明 |
|---|---|---|---|
| **整理** | 每 3 小时（可配）/ 对账前 / 设置里「现在整理一次」 | 后台，放开模式，写操作直接落库（自动 / 待确认）、不问 | **自己一个一次性 Session，不进 `chat.db`**（实现时改的，见 §11）；每次跑记一行 `agent_run` |
| **对账** | 通知上点「现在」、记账页点「现在就说」 | 走授权条 | app 发起一轮（`Item.UserMessage.trigger = true`），对话里画成分隔线「每晚对账」，列出待确认的几笔 |
| **随口** | 你在对话页直接说 | 走授权条 | 查、记、改 |

---

## 6. 工具（模型只能通过它们改数据）

| 工具 | 给谁 | 读/写 | 要点 |
|---|---|---|---|
| `record_expenses` | 整理 | 写 | **一次交一批**：expenses（每笔带 `raw_ids`、金额字符串、方向、类别路径、`ask`、`refund_of`）+ ignore + unreadable。每条通知都要有去处；过不了护栏的只打回那一笔 |
| `list_expenses` | 整理、对话 | 读 | 按时间段 / 类别（任一层，或「未归类」）/ 方向 / 状态 / 关键词 / 标签筛；第一行是合计 |
| `add_expense` | 对话 | 写（问） | 随口记一笔，落库即「你确认过」；对账时补记被遮蔽的通知可带 `raw_ids` |
| `update_expenses` | 对话 | 写（问） | 归类、拆几类、改摘要 / 备注 / 商户、打标签、挂退款、作废、记住商户；改了类别的自动变「你确认过」 |
| `add_category` | 对话 | 写（问） | 用户要新的一级类别时才用；二级在归类时写新名字就会建 |

计划里的 `list_raw_notifications`（整理的输入直接写进那一轮）、`categorize_expenses` / `link_refund` / `split_expense`（并进 `update_expenses`）、`snooze_ledger_check`（「晚点」在通知按钮上）都没做成单独的工具。

---

## 7. 代码守的护栏（落库前硬校验，一条不过整笔打回、退回待整理）

1. **金额必须在原文里出现**：金额换算成字符串，要能在某条 `from_raw_ids` 的正文里找到。挡住绝大多数幻觉。
2. **必须引用原始通知**：`from_raw_ids` 非空、都存在、都属于本批（或最近已整理的）。
3. **同一组 raw id 只能对应一笔**：重跑时按 raw id 覆盖，不新增 —— 天然幂等。
4. **合并后的金额 ≤ 被合并各条之和**。
5. **类别必须已存在**；要新类别只能走 `add_category`。
6. **时间落在通知时刻前后 24 小时内**。

另外两条铁律：**永不删原始通知**；**重跑不覆盖用户确认过的流水**（`confirmed_by_user`）。每笔流水记 `parsed_by`（模型名 + 提示词版本）。

---

## 8. 要注意的地方

1. **自动归错不能固化**：商户记忆只从用户确认来（§1）。
2. **重跑不能盖掉你确认过的**：只重跑「自动」和「待确认」的。
3. **冷却 10 分钟再整理**：一次挡三件事 —— 招行/掌生两条别被拆进两批；遮蔽版有时间补回真正文；丢了的回调有时间被扫回来。
4. **只剩遮蔽版的**：模型读不出金额 → 标「看不清」→ 对账时直接问你金额，不猜。
5. **同一时刻只有一个 agent 写账**：3 小时整理、手动整理、对话对账可能撞车，加锁排队。
6. **分批**：攒多了每批最多 30 条，上一批的结果带进下一批，跨批的合并和退款关联接得上。
7. **失败要退避**：网关挂了等下个周期，别连环重试；整理失败接进顶栏印章状态。
8. **荣耀会拖延后台任务**：「每 3 小时」实际可能 5、6 小时；不影响正确性，21:30 前那次强制整理兜底。倾向用 WorkManager 周期任务 + 联网约束；21:30 对账要准时，用 `ledger/` 自己的闹钟。
9. **`chat.db` 里有账**：工具调用参数（金额、商户）会跟着对话落盘。以后做导出或云备份要记得。

---

## 9. 执行阶段

| 阶段 | 内容 | 依赖 |
|---|---|---|
| 一 · harness 补齐 | `Item.Trigger`（触发开头的一轮）、后台跑一轮（写操作直接落库、不问）、卡片和授权条按工具名分派 | 无 |
| 二 · 建库 | 按 §10 建 `ledger.db` v1（导出 schema）；预设类别写进建库回调；采样器转正（写 `raw_notification`，jsonl 导入后删，临时采样页删掉） | 无 |
| 三 · 整理 agent | 工具 + 护栏（§7 + §10.6）+ 提示词；拿这几天的真实样本全量跑，人工核对 | 一、二 |
| 四 · 对账 / 随口 | 对话里的记账工具、授权条人话、开场白 | 三 |
| 五 · 调度 | 3 小时整理（可配）+ 21:30 对账（可配）+ 通知三按钮 + 提前对完取消 | 三、四 |
| 六 · 流水列表页 | **先出设计稿**；只读，按天分组 | 二 |
| 七 · 真机全链 | 付一笔 → 整理 → 自动归 / 问 → 答 → 记住；第二天同商户不再问；招行 + 掌生只记一笔；杀进程后还在 | 全部 |

每完成一步把真机证据写进 CLAUDE.md「现状」。

---

## 10. 数据格式（2026-09-23 定稿）

**以后所有统计都从这里取。** 原则：存事实、存判断（带上是谁判的），不存能算出来的（星期几、早饭还是夜宵，都从时刻算）。

### 10.1 口径（表是为它服务的）

你在界面上只看三样：**这个月支出多少、收入多少、钱花在哪些一级类别上（吃穿住行）**。更细的问题都在对话里问模型，模型能读到下面所有字段。

- **金额**：整数，单位**分**，永远是正数；正负由方向决定。只记人民币。
- **方向**四种：

  | 方向 | 例子 | 统计里 |
  |---|---|---|
  | `OUT` 支出 | 消费、转给别人 | 加进当月支出 |
  | `REFUND` 退款 | 退货已成功 | **算退款那个月的一笔负数**，类别跟原笔走 |
  | `IN` 收入 | 工资、结息、别人转来 | 加进当月收入 |
  | `TRANSFER` 转移 | 信用卡还款、自己账户互转、余额宝进出 | **哪里都不算**（钱没花出去，只是挪了位置） |

- **月支出 = 当月 OUT − 当月 REFUND**。所以某个类别某个月可能是负数（上个月买、这个月退），照实显示。
- **月收入 = 当月 IN**。
- **算哪个月**：按发生时刻落在本地哪一天（`day` 列）。
- **待确认的也进统计**：金额是真的，只是类别没定。类别没定的归进「未归类」这一桶，不是一个真的类别。
- **作废的**（`VOID`）不进任何统计。

### 10.2 分细靠什么（你看粗的，模型看细的）

| 办法 | 谁看 | 谁来填 | 要不要问你 |
|---|---|---|---|
| **一级类别** | 你（列表页、月度汇总） | 预设，固定 | 新增一级类别要问 |
| **二级类别** | 模型（对话里查） | 模型 | **不问**，自己建；统计时自动加到上级 |
| **标签** | 模型 | 模型 / 你在对话里说 | 不问。「请客」「报销」「冲动」「国庆回老家」都用它，**事件也用标签**，不另设事件表 |
| **商户** | 模型 | 模型认；你确认过才记住默认类别 | 记住默认类别要问（§1 商户记忆） |
| **账户 / 渠道** | 模型 | 模型从通知里读 | 不问 |
| **一笔拆几类** | 你看到的是加总后的结果 | 你在对话里说 / 模型提议 | 要问 |
| **摘要** | **你**：列表页每笔显示这一句 | 模型写 | 不问 |

预设类别（`created_by = PRESET`）：

| 一级（支出） | 预设二级（给模型定个调，它还能自己加） |
|---|---|
| 餐饮 | 外卖、堂食、咖啡饮品、买菜、零食 |
| 交通 | 打车、公共交通、停车、加油、火车飞机 |
| 购物 | 服饰、数码、家居 |
| 日用 | 超市日用、理发、快递 |
| 娱乐 | 电影演出、游戏、会员订阅 |
| 住房 | 房租、水电燃气、物业、话费网费 |
| 医疗 | 药品、门诊 |
| 其他 | —— |

| 一级（收入） | |
|---|---|
| 工资、利息理财、转账红包、其他收入 | 你只看收入总数，这几个是给对话用的 |

只有两层，二级下面不能再挂。

### 10.3 表（`ledger.db`，Room，v1 起 `exportSchema`）

**`raw_notification` 原始通知** —— 唯一真相，永不删

| 列 | 类型 | 说明 |
|---|---|---|
| `id` | INTEGER PK | |
| `pkg` | TEXT | 来源 app 包名 |
| `notif_key` | TEXT | 系统通知 key |
| `post_time` | INTEGER | 通知时刻，epoch ms |
| `title` / `text` | TEXT? | 原样 |
| `extras` | TEXT | 整个 extras 的 JSON，原样 |
| `captured_at` | INTEGER | 我们抓到的时刻 |
| `captured_how` | TEXT | `posted` / `active` / `manual` / `unlock` / `retry+Ns` |
| `fingerprint` | TEXT UNIQUE | `key \| postTime \| text`，去重用 |
| `redacted` | INTEGER | 正文是不是「敏感数据已隐藏」 |
| `state` | TEXT | `PENDING` 待整理 / `DONE` 已整理 / `IGNORED` 不是账 / `UNREADABLE` 看不清 / `SUPERSEDED` 被真正文替代 |
| `state_note` | TEXT? | 忽略的理由（模型写，比如「营销」「还款提醒」） |
| `processed_at` | INTEGER? | |

同一个 `notif_key + post_time` 先来了遮蔽版、后来了真正文 → **代码**把遮蔽版标 `SUPERSEDED`，不用模型判断。

**`txn` 流水** —— 一笔真实发生的钱

| 列 | 类型 | 说明 |
|---|---|---|
| `id` | INTEGER PK | |
| `direction` | TEXT | `OUT` / `IN` / `REFUND` / `TRANSFER` |
| `amount` | INTEGER | 分，> 0 |
| `occurred_at` | INTEGER | 发生时刻，epoch ms |
| `time_basis` | TEXT | `EXACT` 正文里写了时刻（建行）/ `NOTIFIED` 拿通知时刻当发生时刻 / `DAY` 只知道哪天（随口说「昨天」） |
| `day` | INTEGER | 本地日期 `yyyyMMdd`，写入时按当时时区算好；**统计只按它分月** |
| `account_id` | INTEGER? → account | |
| `channel` | TEXT? | 支付宝 / 财付通 / 花呗 / 云闪付 … |
| `merchant_raw` | TEXT? | 通知里的原样商户串，比如「支付宝-山姆会员商店」 |
| `merchant_id` | INTEGER? → merchant | 规范化后的商户 |
| `summary` | TEXT | 模型写的一句人话，列表页显示它 |
| `note` | TEXT? | 你在对话里补的话 |
| `source` | TEXT | `NOTIFICATION` 从通知整理的 / `CHAT` 你随口说的（没有原始通知） |
| `state` | TEXT | `AUTO` 自动归类 / `PENDING` 待确认 / `CONFIRMED` 已确认 / `VOID` 作废 |
| `confidence` | REAL? | 0–1，模型自报，只拿来排序，不当真 |
| `ask` | TEXT? | 待确认时模型想问你的话 |
| `parsed_by` | TEXT | 模型名 + 提示词版本 |
| `created_at` / `updated_at` | INTEGER | |

**`allocation` 分摊** —— **统计从这张表加**，不直接加流水

| 列 | 类型 | 说明 |
|---|---|---|
| `id` | INTEGER PK | |
| `txn_id` | INTEGER → txn | |
| `category_id` | INTEGER? → category | 挂叶子类别；NULL = 未归类 |
| `amount` | INTEGER | 分，> 0 |

不拆的流水就一行，金额等于流水金额。这样一笔拆几类、退款抵回原类别，统计都不用写特殊逻辑。

**`txn_raw` 流水 ↔ 原始通知**

| 列 | 说明 |
|---|---|
| `txn_id` → txn | |
| `raw_id` → raw_notification，**UNIQUE** | 一条通知最多属于一笔流水 —— 天然防重复记账 |

**`txn_link` 流水 ↔ 流水**

| 列 | 说明 |
|---|---|
| `from_id` → txn | 比如退款那笔 |
| `to_id` → txn | 比如原来那笔 |
| `kind` | 先只有 `REFUND`；以后要分期、垫付回款再加 |

**`category` 类别**

| 列 | 说明 |
|---|---|
| `id` | |
| `name` | 同一个上级下 UNIQUE |
| `parent_id` | NULL = 一级 |
| `kind` | `OUT` / `IN` |
| `sort` | |
| `archived` | 停用不删，旧账还挂着 |
| `created_by` | `PRESET` / `MODEL` / `USER` |

**`merchant` 商户** + **`merchant_alias` 别名**

| 列 | 说明 |
|---|---|
| `merchant.id` / `name` UNIQUE | 规范名，比如「山姆会员商店」 |
| `merchant.category_id` | **商户记忆**：只有你确认过才写，模型自己归的不写 |
| `merchant.confirmed_at` | |
| `merchant_alias.alias` PK / `merchant_id` | 原样商户串 → 商户；整理时代码先按 `merchant_raw` 精确查一遍，把记忆喂给模型 |

**`account` 账户**

| 列 | 说明 |
|---|---|
| `id` / `bank` / `tail` / `type` / `label` | `type`：`DEBIT` 储蓄 / `CREDIT` 信用 / `HUABEI` 花呗 / `BALANCE` 余额；`(bank, tail, type)` UNIQUE，整理时代码按模型给的三项 upsert |

**`tag` / `txn_tag` 标签**

| 列 | 说明 |
|---|---|
| `tag.id` / `name` UNIQUE / `created_at` | |
| `txn_tag.txn_id` + `tag_id` 联合主键 | |

**`change_log` 改动历史**

| 列 | 说明 |
|---|---|
| `id` / `txn_id` / `at` | |
| `field` | `state` / `amount` / `allocation` / `merchant` / `tags` / … |
| `old` / `new` | 文本或 JSON |
| `actor` | `MODEL` / `USER` / `CODE` |
| `reason` | 可空；模型改的写理由 |

新建不记（`created_at` + `parsed_by` 够了），**之后的每次改都记**。你问「这笔怎么变成日用了」，模型能答。

### 10.4 统计怎么算

```sql
-- 某月支出，按一级类别；未归类的 top 是 NULL
SELECT COALESCE(c.parent_id, c.id) AS top,
       SUM(CASE t.direction WHEN 'OUT' THEN a.amount ELSE -a.amount END) AS spent
FROM allocation a
JOIN txn t ON t.id = a.txn_id
LEFT JOIN category c ON c.id = a.category_id
WHERE t.state != 'VOID'
  AND t.direction IN ('OUT', 'REFUND')
  AND t.day BETWEEN 20260901 AND 20260930
GROUP BY top;

-- 某月收入
SELECT SUM(amount) FROM txn
WHERE state != 'VOID' AND direction = 'IN' AND day BETWEEN 20260901 AND 20260930;
```

对话里的细查全部走 `list_expenses`，过滤条件：时间段、类别（任一层）、标签、商户、账户、方向、状态、关键词（摘要 / 备注 / 商户原文）；返回明细 + 合计。

### 10.5 索引

`txn(day)`、`txn(state)`、`txn(merchant_id)`、`allocation(txn_id)`、`allocation(category_id)`、`txn_raw(raw_id)` UNIQUE、`raw_notification(state, post_time)`、`raw_notification(fingerprint)` UNIQUE、`merchant_alias(alias)` PK、`txn_tag(tag_id)`。一年几千笔，这些足够。

### 10.6 写入规矩（代码硬校验，补在 §7 后面）

7. **分摊加起来必须等于流水金额**。
8. **分摊挂一级或二级都行**（只写一级 = 没细分），方向要对得上类别的 `kind`（`REFUND` 挂支出类别，`TRANSFER` 不挂类别）。新建二级前查别的一级下有没有同名的，有就打回（9-23 真机上模型建过一个「其他/理发」，而「日用/理发」本来就有）。
9. **一条原始通知只属于一笔流水**（`txn_raw.raw_id` UNIQUE）。作废一笔时先解开它的 `txn_raw`，通知退回待整理。
10. **`source = CHAT` 的流水不走护栏 1、2**（没有原文可对），但必须经授权条，落库即 `CONFIRMED`。
11. **退款挂上原笔时**，退款的分摊照抄原笔的类别；找不到原笔就让模型挑类别，`txn_link` 空着。
12. **重跑**只动 `AUTO` / `PENDING`；按 raw id 集合找到旧流水就地更新，写 `change_log`（`actor = MODEL`）。
13. **二级类别**每个一级下最多 12 个，模型建之前先查有没有同义的。

### 10.7 故意不做的

| 不做 | 理由 / 替代 |
|---|---|
| 必要性、为谁、报销状态等固定维度列 | 你只看大概；需要时模型打标签，对话里查 |
| 事件表 | 用标签，起止时间从挂着的流水算 |
| 周期 / 订阅表 | 问「每月固定花多少」时模型看历史现算 |
| 币种列 | 只记人民币；真遇到外币再加一列（AutoMigration） |
| 预算 | 没提需求 |

### 10.8 对前面几节的影响

- **§1 类别**：改成两层 —— 一级预设 8 个支出 + 4 个收入，新增要问；二级模型自己建、不问。
- **§4 状态机**：去掉「草稿」，流水一落库就是 `AUTO` 或 `PENDING`；另加 `VOID`（作废，拆错、并错、不是账都走它，不删）。
- **§6 工具**：`categorize_expenses` 可以给多条分摊（一笔拆几类）；`add_category` 建二级不问、建一级要问；新增 `tag_expenses`（写，不问）；`list_expenses` 过滤条件见 10.4。
- **§9 阶段二**：按本节建库；预设类别写进建库回调；`pay_samples.jsonl` 导入 `raw_notification` 后删掉。

流水列表页的设计稿在这之后做，等你的要求。

---

## 11. 实现（2026-09-23 起）

### 代码地图

| 在哪 | 干什么 |
|---|---|
| `harness/background/AgentActivity.kt` | 通用：**谁在后台跑**（`running`，顶栏印章看它）、**按名字的锁**（`tryRun` 拿不到就不跑、`queued` 排队）、`LockedTool`（给写工具套锁） |
| `harness/builtin/ledger/` | 纯 Kotlin，JVM 单测覆盖：领域模型、`LedgerBackend` 接口、`LedgerGuard` 护栏、五个工具、`LedgerTools`（哪组工具给哪个 agent）、`LedgerPrompt`（整理 / 对话两段提示词，`VERSION` 写进 parsed_by） |
| `ledger/db/` | `ledger.db` 的表（§10.3 + `agent_run`）、DAO（统计 SQL 在这）、建库时写预设类别 |
| `ledger/LedgerStore.kt` | `LedgerBackend` 的 Room 实现 + 采集入口 `ingest()`（遮蔽版 / 真正文在这里就理清） |
| `ledger/PaySampler.kt` | 通知监听（类名没改，改了要重新授权），直接写库；`LegacySamples` 把调研时的 jsonl 导进来一次，原文件改名留底 |
| `ledger/PaySources.kt` | 听哪几家（加一家 = 加一行）、遮蔽文案、通知使用权 |
| `ledger/organize/` | `Organizer`（先扫通知栏、再数、有才叫模型，整轮拿账本锁）、`OrganizeWorker`（周期 + 现在跑一次） |
| `ledger/check/` | 每晚对账：自己的闹钟、通知三按钮、`CheckReceiver`（含开机 / 覆盖安装重排）、对账那一轮的开场 |
| `ui/ledger/` | 抽屉（方向 B「两张纸」）、记账三层（总览 → 类别 → 一笔）、`LedgerViewModel`（日 / 月 / 季 / 年） |
| `ui/chat/LedgerReceipt.kt` | 对话里记账操作的回执、授权条上的人话、对账分隔线 |

### 和计划不一样的地方

- **整理不进 `chat.db`**：后台一轮一轮的整理写进对话记录会把对话淹掉，而且和前台对话抢同一个 Session。整理用一次性 Session，结果在账本里，过程记在 `agent_run`。
- **锁**：账本一把锁（`LedgerTools.LOCK`）。整理整轮拿着，拿不到就这次不跑；对话里的写工具每次写前排队拿。拿着锁的时候顶栏印外面转一圈细线，纸签上写「后台在整理账目 · N 秒」。
- **对账那一轮**是 `Item.UserMessage(trigger = true)`：模型看到的开头多一句「app 自动发起，不是用户说的」，落盘 kind 是 `TRIGGER`（没改表）。
- 整数金额（「5」）只在挨着「元 / ¥ / 人民币」时才算在原文里 —— 单测抓到「4918」能撞上「尾号4918」。

### 状态

- [x] 编译、24 条 JVM 单测（护栏、合并、幂等重跑、确认过不动、拆分、随口记、同批退款）
- [x] **真机（9-23，火山方舟 deepseek-v4.1-flash）**：
  - 42 行调研样本 → 导成 23 条原始通知（遮蔽版 1 条自动标 SUPERSEDED，空通知 2 条忽略）
  - 整理一轮 15 秒：招行 + 掌生三对各合成一笔；7 条营销 / 优惠券忽略；山姆 → 日用/超市日用、结息 → 利息理财自动归；
    其余 8 笔没商户的标待确认、各附一句问话；退款 1.49 挂上原笔 3.00（模型没指，靠同商户兜底对上）。连跑三次结果一致
  - 顶栏：抽屉键 / 印章换位；整理时印外转圈、纸签「后台在整理账目 · N 秒」
  - 抽屉两张纸、记账总览（822.62 / 1.73，和设计稿手算一致）、未归类那一层、一笔的详情（依据是建行原文）
  - 一笔详情「去对话里说」→ 输入框预填「关于 #8（…）：」→ 说一句 → 改好、回执
  - 记账页「现在就说」→ 分隔线「每晚对账」→ 一次问两笔 → 回答后作废 1 笔、归类 3 笔、退款跟着原笔改类别，改动记录齐全
  - 21:30 的对账闹钟在 `dumpsys alarm` 里（`CHECK_FIRE`，精确）
- 真机上踩出来、已修：柱子日期标签被截；模型另建「其他/理发」「其他/话费」（对话 agent 看不到类别表）→
  允许直接挂一级、近似名打回、对账开场附类别表、对话提示词列出预设二级、顺手建的二级空了就停用
- [ ] 没验：真付一笔 → 实时进库 → 3 小时后自动整理；21:30 通知和「晚点」「今天算了」；授权条上的记账（手机现在是放开模式）；深色
- 测试后账本清空重来了一次（我在对账里答的是编的），现在库里是模型整理的原样，8 笔等你 21:30 答。**对话记录里留着我那几轮测试**（「it was a haircut」等）
